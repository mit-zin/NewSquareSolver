#ifndef REQUEST_H
#define REQUEST_H

#include "result.h"
#include "colortext.h"
#include "Utilities.h"

void        ArrClear(char arr[], int arSize);
enum result Request (enum yes_no * answer);

#endif
